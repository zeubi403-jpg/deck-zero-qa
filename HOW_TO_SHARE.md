# HOW TO SHARE — share_v2 (QA franchise build)

Supersedes polish notes for **this tree only**. Live Ashen-Null pack remains in `docs/share/` + `docs/share_ashen_null_lock/`.

**Viewport 1:** Deck Zero knives (unchanged Brief law).  
**Deep:** `ARCHIVES_Silent_Running.html` — full Silent Running novella + Vurtek stills.  
**Interactions:** Keep Ashen-Null glass — AUD / LO / hum (`dz-terminal.js`), crush ring on Archives, Pitch crush/scar unchanged.  
**QA deploy:** `tools/_share_deploy/deploy_share_v2_qa_pages.ps1` → separate `deck-zero-qa` Pages. **Do not** run production `deck-zero` deploy until cutover.  
**Novel:** English master prose in `media/archives/silent_running.json`. Re-export: `python tools/_export_silent_running_json.py`.  
**Soft→Bridge F5** still gates play SIGN.
